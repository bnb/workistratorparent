name: Hello from workistrator

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from ${{ github.repository }} - this workflow is managed by bnb/workistratorparent via workistrator."
